/*
 * template is a common structure of several class. so, 
 *   class --template for-- object
 *   "template" --template for-- class
 *
 *
 * in template a <> is used to replace with different keywords and make various type of class using the same template
 * syntax:-
 *      template <a lebel(which will be used later, to rrplace with c++ keywords)>
 *      class class_name {
 *      //full class syntax but where needed use the <> brace enclosed label
 *      };
 *
 *  make a template and use it to enhance code reusability and DRY principle
 *
 *  c++ provides STL(Standerd Template Library) for template and STL or custom template is needed mostly in the field of competitive programming field
 *
 */
#include <iostream>

using namespace std;

int main() {
    return 0;
}

